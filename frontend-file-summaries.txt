{
  "frontend/src/pages/Blog.tsx": "Blog listing page that fetches and displays all blog posts from the backend, using `useNavigate` with typed params instead of a dynamic template literal in the `to` prop to fix the TypeScript routing error.",
  "frontend/src/pages/admin/Dashboard.tsx": "Admin dashboard page protected by AdminGuard, showing stats (projects, leads, blog count) fetched from the backend and navigation cards to all admin management sections.",
  "frontend/src/pages/admin/ProjectsManagement.tsx": "Admin projects management page with sidebar, search, pagination, category dropdown, and CRUD using individual hooks (useAddProject, useUpdateProject, useDeleteProject) instead of the removed useProjectMutations.",
  "frontend/src/pages/admin/BlogManagement.tsx": "Admin blog management page with sidebar, search, pagination, and CRUD using individual hooks (useAddBlog, useUpdateBlog, useDeleteBlog) instead of the removed useBlogMutations.",
  "frontend/src/pages/admin/TestimonialsManagement.tsx": "Admin testimonials management page with sidebar, search, pagination, and CRUD using individual hooks (useAddTestimonial, useUpdateTestimonial, useDeleteTestimonial) instead of the removed useTestimonialMutations.",
  "frontend/src/pages/admin/SkillsManagement.tsx": "Admin skills management page with sidebar, search, pagination, category select, and CRUD using individual hooks (useCreateSkill, useUpdateSkill, useDeleteSkill) instead of the removed useSkillMutations.",
  "frontend/src/pages/admin/ServicesManagement.tsx": "Admin services management page with sidebar, search, pagination, and CRUD using individual hooks (useAddService, useUpdateService, useDeleteService) instead of the removed useServiceMutations.",
  "frontend/src/pages/admin/LeadsManagement.tsx": "Admin leads management page with sidebar, search, pagination, and delete using the individual useDeleteLead hook instead of the removed useLeadMutations.",
  "frontend/src/pages/admin/SeoManagement.tsx": "Admin SEO settings management page with sidebar, search, pagination, and CRUD using individual hooks (useSetSeoSetting, useDeleteSeoSetting) instead of the removed useSeoMutations.",
  "frontend/src/App.tsx": "Main app router with public layout (nav+footer) and admin layout as separate nested route groups; removed the non-existent AdminLogin import and uses /admin as the dashboard route directly.",
  "frontend/src/components/SEOHead.tsx": "SEO head component that sets document title and meta description; fixed import to use the correct `useGetSeoSettingByPage` hook name.",
  "frontend/src/pages/BlogPost.tsx": "Blog post detail page that reads the slug using `useParams({ strict: false })` to avoid route path mismatch errors with the nested routing structure.",
  "frontend/src/pages/admin/Help.tsx": "Admin-only help guide page with step-by-step instructions for all admin operations, protected by an inline AdminGuard and featuring the AdminSidebar.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions with explicit identity checks on all mutations; adds `useGetServices` as a backward-compatible alias for `useListServices`, matching the existing alias pattern used for `useGetSkills` and `useGetCategories`.",
  "frontend/src/components/AdminGuard.tsx": "Admin route guard using React Query scoped to the current principal for cached admin status; fixes the `login().catch()` type error by wrapping the `login()` call in a plain try/catch block since it returns `void`.",
  "frontend/src/components/LoginButton.tsx": "Login/logout button that invalidates `isAdmin` and `actor` query keys after login so AdminGuard immediately re-evaluates admin status with the fresh authenticated identity."
}